import StudentDashboard from '../StudentDashboard.jsx';

// Controller entry for future state/action extraction.
export default function StudentDashboardController(props) {
  return <StudentDashboard {...props} />;
}
