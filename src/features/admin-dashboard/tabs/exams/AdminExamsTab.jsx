// Placeholder boundary for the heavy exams tab UI.
// The current UI still renders through the modular tabs adapter until each block is extracted safely.
export default function AdminExamsTab({ children }) {
  return <>{children}</>;
}
