# Tabs UI Organization Patch

تم تطبيق تنظيم بالتبويبات لتقليل زحمة الصفحات وإظهار كل مجموعة معلومات عند الضغط فقط.

## أهم التعديلات
- إضافة مكوّن `InlineTabs` داخل `src/app/AppRoot.jsx`.
- تقسيم Dashboard الأدمن إلى:
  - نظرة عامة
  - تحليل الأداء
  - تحليل الأسئلة
  - لوحة الشرف
- تقسيم الاشتراكات والدفع إلى:
  - طلبات الدفع
  - إدارة الاشتراكات
- تقسيم البث المباشر / AI إلى:
  - الحصص المباشرة
  - استخدام AI
  - رؤى AI
- تغليف الحماية، بنك الأسئلة، والواجبات بنفس نمط التبويبات لإبقاء الواجهة موحدة.

## أوامر التشغيل
```bash
npm install
npm run build
npm run dev
```

## أوامر الرفع المقترحة
```bash
npm install
npm run build
vercel --prod
```
