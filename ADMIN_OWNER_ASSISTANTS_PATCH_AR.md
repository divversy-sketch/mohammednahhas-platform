# تعديل صلاحيات المالك والمساعدين

## ما الذي تم إصلاحه؟

- تم تثبيت الحساب `mido16280@gmail.com` كمالك أساسي للمنصة بدون الاعتماد على وجود مستند مسبق في `admins`.
- عند تعيين أي طالب كمساعد من صفحة **صلاحيات الأدمن** يتم الآن تحديث مكانين معًا:
  - `admins/{uid}` لتفعيل دخوله إلى لوحة الإدارة.
  - `users/{uid}` لتحويل ظهوره من طالب إلى مساعد داخل قوائم الإدارة.
- تم إضافة ملف مركزي للصلاحيات: `src/config/adminPermissions.js`.
- تم فلترة تبويبات لوحة الإدارة حسب صلاحيات المساعد، بحيث يرى فقط الأقسام المحددة لدوره.
- تم منع إلغاء صلاحية حساب المالك الأساسي من الواجهة.
- تم تحديث قواعد Firestore وRealtime Database لدعم المالك الأساسي وصلاحيات المساعدين.

## الأدوار المتاحة

- مدير عام
- مشرف امتحانات
- مشرف طلاب
- مشرف محتوى
- مشرف اشتراكات ودفع
- دعم فني

## طريقة التشغيل

```bash
npm install
npm run build
```

## النشر المطلوب بعد رفع التعديل

```bash
firebase deploy --only firestore:rules
firebase deploy --only database
npm run build
firebase deploy --only hosting
```

> مهم: لا ترفع مجلد `node_modules`، المشروع يعتمد على `package.json` و`package-lock.json` فقط.
