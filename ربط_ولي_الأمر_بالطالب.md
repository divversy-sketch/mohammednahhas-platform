# ربط حساب ولي الأمر بالطالب

1. أنشئ حسابًا لولي الأمر من Firebase Authentication بالبريد وكلمة المرور.
2. انسخ UID الخاص بحساب ولي الأمر.
3. من Firestore أنشئ مستندًا داخل مجموعة `users` ويكون اسم المستند هو UID ولي الأمر.
4. أضف الحقول التالية:

```json
{
  "name": "اسم ولي الأمر",
  "email": "parent@example.com",
  "role": "parent",
  "status": "active",
  "linkedStudentIds": ["STUDENT_UID"]
}
```

5. استبدل `STUDENT_UID` بالـ UID الخاص بالطالب الموجود في Authentication أو باسم مستند الطالب داخل `users`.
6. عند دخول ولي الأمر بنفس البريد وكلمة المرور، ستفتح له لوحة ولي الأمر تلقائيًا بدل بوابة الطالب.

## ظهور الغياب

يُسجل كل حضور داخل مجموعة `attendance_records` بهذا الشكل:

```json
{
  "studentId": "STUDENT_UID",
  "studentName": "اسم الطالب",
  "sessionTitle": "حصة النحو",
  "status": "absent",
  "date": "Firestore Timestamp"
}
```

القيم المسموحة للحالة:
- `present` حاضر
- `absent` غائب
- `late` متأخر

نتائج الامتحانات والواجبات تظهر تلقائيًا من البيانات الموجودة بالفعل في `exam_results` و`assignment_submissions`، ولا تحتاج إدخالًا مزدوجًا.
