# Admin Page Restructure Patch

## التعديلات المنفذة
- دمج طلبات الدفع مع إدارة الاشتراكات داخل تبويب واحد: `الاشتراكات والدفع`.
- حذف تبويبات الأدمن غير المطلوبة من القائمة: تحليلات AI، AI Lab، AI Insights، رفيقك في العربي، الرسائل، رسائل الطلاب، إشعارات الطلاب.
- حذف استيراد واستخدام مكونات رفيقك في العربي ورسائل الطلاب من التطبيق.
- حذف ملفات مكونات رفيقك في العربي ورسائل الطلاب من `src/features`.
- حذف API endpoints الخاصة بمساعد/فحص AI: `api/ai-coach.js` و `api/ai-check.js`.
- دمج تحليل الطلاب، تحليل الأسئلة، ولوحة الشرف داخل Dashboard شامل.
- دمج الامتحانات والنتائج داخل تبويب واحد: `الامتحانات والنتائج`.
- تحويل التواصل الظاهر للطالب إلى زر واتساب فقط برقم المنصة المسجل.
- إزالة ChatWidget القديم من HomeWidgets واستبداله بزر واتساب مباشر في صفحات الطالب/الزائر/الدخول.

## ملاحظة اختبار
- لم يتم تضمين `node_modules` في الباتش.
- أمر البناء المطلوب بعد فك الضغط: `npm install && npm run build`.
